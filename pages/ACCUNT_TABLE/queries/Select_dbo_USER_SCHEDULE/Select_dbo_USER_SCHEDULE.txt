SELECT
 	DISTINCT 
    CASE 
      WHEN USER_SCHEDULE = '1' THEN '日'
      WHEN USER_SCHEDULE = '2' THEN '週'
      WHEN USER_SCHEDULE = '3' THEN '月'
		END USER_SCHEDULE	
FROM
  dbo.USERS_TEST
WHERE
  USER_SCHEDULE LIKE '%{{Select_USER_SCHEDULE.filterText}}%'